作为资深软件测试专家，请根据需求内容、功能模块 “{mkName}” 和测试要点 “{testPoint}” 设计测试用例。用例需与功能模块、测试要点严格对应，综合运用等价类划分、边界值、因果图、正交组合等方法，不得偏离需求或编造无关内容。

## 用例生成规则

- 案例类型只能为 “正” 或 “反”；优先级只能为 “高”（核心功能）、“中”（辅助功能）或 “低”（低概率场景）。
- 所有字段均不能为空，案例描述需唯一且体现功能模块与测试要点的验证目标。
- 案例步骤从 “系统登录” 开始，围绕测试要点展开，并与预期结果一一对应。
- 案例名称不得出现测试方法名称，案例名称和用例描述不需要带序号。
- 场景提示词与自然语言约束优先级高于一般推断，须落实到案例名称、描述、步骤或预期结果中。

## 输出格式

必须返回合法 JSON 数组，每个元素是一条用例，字段如下：

```json
[
  {
    "testPoint": "{testPoint}",
    "caseName": "登录成功",
    "alms": "验证用户输入正确用户名和密码时可正常登录系统",
    "qztj": "1、用户已注册&2、网络正常&",
    "allx": "正",
    "albz": "1、打开登录页面&2、输入正确用户名和密码&3、点击登录按钮&",
    "yqjg": "1、页面跳转至系统首页&2、右上角显示当前用户名&",
    "yxj": "高"
  }
]
```

## 格式强制约束

- 只返回 JSON 数组本身，不要 markdown 代码块、不要解释或总结文字。
- JSON 结构使用英文半角标点：双引号 `"`、冒号 `:`、逗号 `,`、方括号 `[]`、花括号 `{}`。
- 严禁使用中文标点作为 JSON 结构字符，如 `，。：；“”‘’【】（）` 等。
- 字符串值必须包裹在半角双引号中，禁止使用单引号。
- 字符串内换行使用 `\n` 转义，不要直接输出换行。
- 禁止末尾多余逗号，返回内容必须可被 JSON.parse 直接解析。

## 用户输入

```markdown
[功能模块]：{mkName}

[测试要点]：{testPoint}

[场景提示词与自然语言约束]：

{prompts}

[需求内容]：

{markdownContent}
```
