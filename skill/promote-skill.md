作为资深软件测试专家，请根据用户输入的完整需求内容、指定功能模块 “{mkName}” 及对应测试要点 “{testPoint}”，设计多条详尽的测试用例，设计方法遵循等价类划分、边界值、因果图、正交组合等常见软件测试方法，确保测试用例与功能模块、测试要点严格一一对应，不偏离核心需求，并按照指定 JSON 格式输出。案例输出要求：

根据以上需求分析说明书生成测试案例（例如序号3.1为系统模块，3.1.1为功能点模块，最终生成的功能点强制与功能点模块绑定，不得胡编乱造，先根据需求书划分不同的系统，再根据系统分不同的功能点模块，再根据功能点模块中的描述与处理规则生成测试案例，生成的测试案例中的功能点要与需求分析书中的功能点模块一致，测试案例中的功能点，案例名称和用例描述不需要带序号）。

案例类型仅分为正、反两种，生成结果只能是 “正” 或 “反”，不能为空；

优先级分为高、中、低三种，不能为空，核心功能验证案例设为 “高”，辅助功能设为 “中”，低概率场景设为 “低”；

每个案例需有唯一且不重复的案例描述，不能为空，描述需明确体现 功能模块 “{mkName}” 和测试要点 “{testPoint}” 的验证目标；

案例步骤不能为空，需从 “系统登录” 开始详细描述每一步操作，所有操作均围绕测试要点 “{testPoint}” 展开，保障步骤完整可复现；

案例步骤和预期结果（不能为空）数量需一一对应，预期结果需精准回应测试要点“{testPoint}” 的验证需求，不出现与要点无关的表述；

案例名称不得出现测试方法名称，每个字段（案例名称、案例类型、优先级、案例描述、案例步骤、预期结果）均不能为空；



**以下为用户在「动态指令」中配置的场景提示词与自然语言约束，优先级高于一般推断，生成前须逐条核对是否已写入案例：**

```

{prompts}

```

若上述约束中包含场景提示词，则每条案例至少须在 **案例名称、案例描述（alms）、案例步骤或预期结果** 之一中明确体现该场景要求，不得生成与场景无关的泛化用例。

若上述约束中包含自然语言描述，则须将其要点拆解并落实到具体案例的步骤、前置或预期中，不得忽略或仅用空洞表述敷衍。



所有测试用例仅对应功能模块 “{mkName}” 和 测试要点 “{testPoint}”，不掺杂其他模块或要点的内容，确保一一对应无错乱。

testPoint是测试要点，caseName是案例标题，alms是案例描述，qztj是前置条件，allx是案例类型，albz是案例步骤，yqjg是预期结果，yxj是优先级，输出json格式如下：

```

[

    {

        "testPoint":"",

        "caseName":"",

        "alms":"",

        "qztj":"1、xxxx&2、xxxx&",

        "allx":"",

        "albz":"1、xxxx&2、xxxx&",

        "yqjg":"1、xxxx&2、xxxx&",

        "yxj":""

    }

]

```



用户的输入需求内容和要求如下：

```

[功能模块]：{mkName}

[测试要点]：{testPoint}

[场景提示词与自然语言约束]：

{prompts}

[需求内容]：

{markdownContent}

```



