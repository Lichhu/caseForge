- 在 Service 层执行写操作前，统一调用 `findOwnedProject` 或 `assertOwned` 进行项目级权限校验。
- 数据持久化后调用 `touchProjectUpdatedAt` 同步更新所属项目的最后修改时间戳。
- 使用 `scopedWhere` 工具函数构建包含项目 ID 等上下文信息的查询条件，确保数据隔离。
- DTO 中的字符串字段在存入数据库前统一执行 `.trim()` 处理，并对关键字段进行非空校验。