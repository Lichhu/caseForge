- DTO 类统一使用 `@IsOptional()` 配合具体类型约束（如 `@IsString`, `@MaxLength`）来定义可选字段。
- 服务层方法在执行更新或删除前，普遍调用 `findOwnedProject` 或 `scopedWhere` 以确保数据访问符合用户权限范围。
- 针对 `api-test` 平台的项目，在创建和更新时强制执行 `normalizeRequirementNo` 逻辑，确保需求编号格式统一且唯一。