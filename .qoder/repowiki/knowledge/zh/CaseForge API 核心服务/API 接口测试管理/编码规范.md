- 所有数据库查询均通过 `scopedWhere` 工具函数自动附加 `projectId` 过滤条件，确保多租户数据隔离。
- 实体更新时统一调用 `auditFieldsForUpdate()` 注入审计字段（如 modifiedBy, updatedAt），保证操作可追溯。
- 测试执行中的敏感请求头（如 Authorization, Token）在存入运行记录前会通过 `redactHeaders` 进行脱敏处理。
- API 路径与请求体中的变量占位符（如 {{var}}）在执行前通过 `substituteDeep` 进行递归深度替换，支持环境变量与密钥注入。