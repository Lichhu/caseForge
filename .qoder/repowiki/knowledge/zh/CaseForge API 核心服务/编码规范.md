- 所有业务模块均遵循 Controller-Service-Entity 分层架构，并通过 index.ts 统一导出模块定义。
- 通用能力（如审计、AI 工作流、MinIO 客户端）封装在 src/common 目录下，以独立 Module 形式供业务模块引用。
- 环境变量通过 @config/configuration.ts 统一映射为强类型 AppConfig 对象，并在 ConfigModule 中全局注入。