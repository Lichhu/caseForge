- 实体类中布尔字段（如 isActive, isSystem）统一使用 tinyint 类型配合自定义 transformer 进行存取转换。
- 服务层在执行写操作前强制进行所有权校验（assertOwned）或唯一性检查（ensureScenarioNameUnique）。
- DTO 定义中广泛使用 ApiProperty 装饰器以自动生成 Swagger 文档，并结合 IsString, MaxLength 等约束确保输入合法性。