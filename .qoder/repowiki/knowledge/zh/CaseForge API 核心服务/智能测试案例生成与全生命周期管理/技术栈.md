- **异步队列**：基于 TypeORM 实体 `CaseGenerateJobEntity` 实现的数据库轮询队列，配合内存并发槽（`case-generate-concurrency.ts`）控制 AI 调用速率。
- **文件处理**：使用 `jszip` 手动构建符合 XMind 2020+ 规范的 `.xmind` 文件结构；使用 `exceljs`（隐含在 `test-platform-case-excel.util.ts` 中）处理 Excel 导入导出。