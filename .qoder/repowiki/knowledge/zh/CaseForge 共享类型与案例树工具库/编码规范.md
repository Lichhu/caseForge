- 采用扁平化目录结构，所有功能模块直接置于 `src/` 根目录下，通过 `index.ts` 统一暴露 API。
- 类型定义优先使用 `type` 别名处理联合类型（Union Types），使用 `interface` 描述对象结构，并在 `index.ts` 中集中声明基础契约。
- 工具函数遵循纯函数设计原则，避免副作用，例如 `cloneCaseTree` 和 `sanitizeCaseTitleText` 均返回新对象或字符串而非修改入参。