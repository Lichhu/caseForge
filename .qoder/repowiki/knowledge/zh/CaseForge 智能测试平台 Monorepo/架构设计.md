- 采用 pnpm workspace 管理 `apps/*`（api, web）与 `packages/*`（shared）的依赖关系，实现跨模块类型共享与版本一致性。
- 根目录 `package.json` 定义统一工作流脚本（如 `dev:api`, `dev:web`），强制在启动子应用前预构建 `@case-forge/shared` 包，确保类型契约同步。
- 通过 `pnpm --filter` 机制隔离各子模块的运行环境与依赖安装，避免全局污染并提升构建效率。