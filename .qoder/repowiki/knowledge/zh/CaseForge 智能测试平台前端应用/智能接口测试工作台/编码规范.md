- 所有业务逻辑与异步数据操作均封装在 `useApiTestStore` 中，组件仅负责 UI 渲染与事件分发。
- JSON 类型的配置字段（如请求报文、环境变量）在表单中统一使用 `textarea` 配合 `JSON.stringify/parse` 进行序列化与校验。
- 涉及删除或覆盖等高风险操作时，统一调用 `ant-design-vue` 的 `Modal.confirm` 进行二次确认。
- 列表类组件（如交易码、案例、执行集）普遍采用“左侧列表导航 + 右侧详情/操作面板”的双栏布局模式。