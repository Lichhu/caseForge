- 采用 `PlatformLayout` 作为通用布局壳，根据路由元信息动态切换“用例生成”与“接口测试”子平台的视觉上下文。
- 通过 `vue-router` 的嵌套路由将不同业务域（`/case-forge`, `/api-test`）隔离在独立视图组件中，实现功能模块的物理拆分与逻辑聚合。
- 利用 `Pinia` 进行跨组件状态管理，配合 `userContext` 工具类在路由守卫层面统一处理用户身份同步，确保多工作台间的会话一致性。