- API 客户端统一在请求拦截器中从 `userContext` 获取用户名并写入 `X-User-Name` 请求头。
- 路由切换时通过 `beforeEach` 钩子自动捕获并持久化 URL 查询参数中的 `userName`。
- Store 中的长耗时任务（如结构化、案例生成）采用前端状态标记配合后端轮询机制更新进度。
- 全局主题配置集中在 `theme/caseForgeTheme.ts`，并通过 `a-config-provider` 在根组件下发。